<?php

/**
 * Check Token in redirection Url.
 * See comment for method in Wrapper
 * Both params are $_GET params
 * 
 * @param string $token 
 * @param string $redirecturl as base64 
 * 
 */
\Radisso\Client::checkToken($token, $redirecturl);
